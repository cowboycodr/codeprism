{
  "name": "@codeprism/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@codeprism/db": "0.1.0",
    "@codeprism/git": "0.1.0",
    "@codeprism/runner": "0.1.0",
    "@codeprism/shared": "0.1.0",
    "@codeprism/ssh": "0.1.0"
  }
}
